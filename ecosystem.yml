apps:
    - script: dist/index.js
      name: main-bot
      cwd: ./
      env:
          NODE_ENV: "development"
      env_production:
          NODE_ENV: "production"
      node_args: --unhandled-rejections=strict
