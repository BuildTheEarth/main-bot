version: "3.9"
services:
  main-bot:
    container_name: main-bot
    build: .
    environment:
        CONFIG_FOLDER: "/etc/buildtheearth/main-bot/config"
    volumes:
      - type: bind
        source: "${CONFIG_FOLDER}"
        target: /etc/buildtheearth/main-bot/config
